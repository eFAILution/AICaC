# Core - token counting
tiktoken>=0.5.0

# AI Providers (for performance measurement)
anthropic>=0.18.0
openai>=1.0.0
ollama>=0.1.0
groq>=0.4.0

# TOON encoding (Token-Oriented Object Notation)
toon_format @ git+https://github.com/toon-format/toon-python.git

# Analysis
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0

# Utilities
pyyaml>=6.0

# Transitive pins — not used directly, but constrain what can resolve
# through the direct deps above. Each clears an OSV advisory that was
# otherwise surfaced during security scans.
#
# Pillow: pulled by matplotlib. 12.3.0 clears a large batch of advisories
# affecting 12.2.x and earlier — decompression-bomb bypasses in the BDF/
# PCF/GD/PDF font+image loaders, heap OOB read/write in paste/crop,
# RankFilter and ImageCmsTransform, JPEG2000 tiled-decode DoS, and the
# WindowsViewer OS command injection (GHSA-4x4j-2g7c-83w6).
Pillow>=12.3.0

# tqdm: pulled by several AI SDK deps. Minimum clears
# GHSA-r7q7-xcjw-qx8q (arbitrary code exec) and -g7vv-2v7x-gj9p
# (CLI argument injection).
tqdm>=4.66.3

# zipp: pulled transitively through importlib-metadata. Minimum clears
# GHSA-jfmj-5v4g-7637 (DoS via malformed zip).
zipp>=3.19.1

# idna: pulled transitively by the AI SDK HTTP stacks (anthropic/openai).
# 3.15 clears GHSA-65pc-fj4g-8rjx — idna.encode() inputs that bypass the
# CVE-2024-3651 fix.
idna>=3.15
